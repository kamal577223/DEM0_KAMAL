import { Meta } from '@storybook/react';
import Navbar from '../components/addstudent_button';
export default {
  title: 'addstudent_button',
  component: Navbar,
} as Meta;


export const Default = () => <Navbar />;